We analyze US gun murders data collected by FBI.

download-data.R - downloads csv file to data subdirectory of murders directory

wrangle-data.R - creates a derived dataset and saves as R object in rda subdirectory of murders directory.

analysis.R - generates a plot and saves it into the fig sub directory of murders directory